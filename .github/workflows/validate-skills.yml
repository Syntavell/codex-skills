name: Validate Skills

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  validate:
    uses: Syntavell/.github/.github/workflows/validate-codex-skills.yml@main
    with:
      validation-command: python scripts/validate_skills.py
